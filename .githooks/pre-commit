#!/usr/bin/env bash
# .githooks/pre-commit
# =====================================================================
# Pre-commit placeholder.
#
# The branch/task-ID check that used to live here was broken -- it read
# $1 expecting a commit message path, but pre-commit is invoked with no
# arguments. That validation now lives in .githooks/commit-msg, which
# is the correct hook for message-level checks. audit 17 F#3.
#
# This hook is intentionally a no-op today. Add pre-commit checks here
# that inspect STAGED FILE CONTENT (lint, secret scan, etc.) — those run
# before the message is prepared and do not need $1.
# =====================================================================

exit 0
